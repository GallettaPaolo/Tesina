<!DOCTYPE html>
<html>
<% include ./partials/header %>

  <body>


    <div class="row">

      <div class="col s12 m3 l3">

        <% include ./partials/sideNav %>
      </div>

       <div id="askmore" class="modal">
          <div class="modal-cont    console.log("Devo sloggare");ent">
            <h4>Un'ultima cosa!</h4>
            <p>Per utilizzare Athager&copy; devi inserire gli ultimi dati mancanti </p>
            <div class="container">
              <form>
                <div class="row">
              <div class="col s12 rolesDiv">
                <div class="input-field">
                  <select>
                    <option value="" disabled selected>Scegli un ruolo</option>
                    <% roles.forEach(function(role){ %>
                    <option  value=<%= role.name %>><%= role.name %> </option>
                    <%});%>
                  </select>
                  <label>Ruolo</label>
                </div>
              </div>
              <div class="input-field col s12 m5 hide specDiv">
                <div class="input-field">
             <select>
                    <option value="" disabled selected>Scegli specialità</option>
                    <% categories.forEach(function(category){ %>
                    <option  value=<%= category.categoryName %>><%= category.categoryName %> </option>
                    <%});%>
                  </select>
                  <label>Specialità</label>
                  </div>
            </div>
            </div>
              </form>
            </div>
          </div>
          <div class="modal-footer">
            <a href="#!" class="addInfo modal-action modal-close waves-effect waves-green btn-flat">Aggiungi informazioni</a>
          </div>
        </div>

      <div class="content col s12 m9 l9">
        <h1><%= actions[actions.length-1].action %></h1>
  
          <%
          var splitRows = 0; 
          competitions.forEach(function(competition){
           %>
           <% if(splitRows%3 == 0){%>
              <div class="row">
            <%} %>
           <div class="col s12 m4">
              <div class="card blue-grey darken-1 waves-effect">
                <div class="card-content white-text">
                  <span class="card-title"><%= competition.description %></span>
                  <ul>
                    <li class="scale">Livello: <%= competition.scale %></li>
                    <li class="date">Data: <%= competition.date %>/17</li> 
                    <li class="location">Località: <%= competition.location %></li>
                  </ul>
                </div>
                <div class="card-action">
                  <a class="subscribe waves-effect" data-code=<%= competition._id%>>Iscriviti</a>
                </div>
              </div>
         </div>
          <%
          splitRows++; 
          if(splitRows%3 == 0){%>
              </div>
            <%} %>
           <% }) %>
      </div>
      </div>
    </div>
    <% include ./partials/footer %>
    <script src="javascripts/main.js"></script>
  </body>

</html>
